/**
 * Route-local types for the dashboard pages.
 *
 * The `ExamItem` / `ExamTab` pair that used to live here described three
 * hard-coded fixture exams. The student test list is served by `GET /me/tests`
 * now and typed by its response schema, so the shapes are gone rather than left
 * behind for someone to reuse.
 */
export {};
